// Package markdown is a minimal Markdown→HTML converter.
// Supports: headings, paragraphs, fenced code blocks, links, *emphasis*, **strong**.
package markdown

// Convert returns the HTML rendering of the given Markdown source.
func Convert(src []byte) []byte {
	// TODO
	return nil
}
